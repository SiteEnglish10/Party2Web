# 复制本文件为 .env 并填入真实值（.env 已被 .gitignore 排除，不会进仓库）
# 生产环境务必设置强口令与随机密钥！

# 管理员账号 / 密码
ADMIN_USERNAME=root
ADMIN_PASSWORD=请改成你的强密码

# 会话签名密钥（生成随机值： python -c "import secrets;print(secrets.token_hex(32))" ）
SECRET_KEY=请改成一段随机长字符串
