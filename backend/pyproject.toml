[project]
name = "party2web-backend"
version = "0.1.0"
description = "便利工具站后端 (FastAPI + SQLite)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlmodel>=0.0.22",
    "python-multipart>=0.0.9",
    "itsdangerous>=2.2",
    "nh3>=0.2",
]

[tool.uv]
package = false
